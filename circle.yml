machine:
  node:
    version: 4.2.6

general:
  branches:
    ignore:
      - gh-pages
  artifacts:
    - "coverage/"

deployment:
  production:
    branch: master
    commands:
      - npm run release
      - npm run codeclimate
