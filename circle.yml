machine:
  node:
    version: 4.3.1

general:
  branches:
    ignore:
      - gh-pages
  artifacts:
    - "coverage/"

deployment:
  production:
    branch: master
    commands:
      - npm run release
      - npm run codeclimate
